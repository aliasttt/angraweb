# چک‌لیست Search Console و سئو بعد از مهاجرت

## ✅ کارهایی که در کد انجام شده

### ۱. ریدایرکت ۳۰۱ (آدرس قدیمی → جدید)
اگر سایت قبلاً استاتیک بود و آدرس‌ها به صورت `.html` بود، الان این ریدایرکت‌ها فعال است:

| آدرس قدیمی   | آدرس جدید   |
|-------------|-------------|
| `/index.html`  | `/` |
| `/about.html`   | `/about/` |
| `/contact.html`| `/contact/` |
| `/services.html` | `/services/` |
| `/packages.html` | `/packages/` |
| `/projects.html` | `/projects/` |
| `/resume.html`   | `/resume/` |

**نتیجه:** لینک‌ها و رتبهٔ قدیمی از دست نمی‌رود.

### ۲. Sitemap XML
- آدرس برای ثبت در گوگل: **`https://YOUR-DOMAIN.com/sitemap.xml`**
- این آدرس همهٔ صفحه‌های مهم (ثابت + سرویس‌ها، پروژه‌ها، بلاگ) را با `lastmod` برمی‌گرداند.

---

## 📋 کارهایی که خودت باید انجام بدی

### ۱. Search Console
- اگر دامنه و پروتکل (https) عوض نشده → **نیازی به ثبت مجدد Property نیست.**
- برو **Search Console → Sitemaps**
  - اگر sitemap قبلی (مثلاً مربوط به سایت استاتیک) داری، حذفش کن.
  - sitemap جدید را اضافه کن:  
    **`https://YOUR-DOMAIN.com/sitemap.xml`**  
    (فقط همون آدرس را در کادر Add a new sitemap بزن و Submit کن.)

### ۲. URL Inspection و Request Indexing
برای چند صفحه مهم:
- صفحهٔ اصلی: `/`
- یک صفحه سرویس: مثلاً `/services/` یا `/web-design/`
- یک مقاله بلاگ: مثلاً `/blog/SLUG/`

برو **URL Inspection** → آدرس را وارد کن → **Request Indexing** بزن تا گوگل زودتر ایندکس کند.

### ۳. Google Analytics
- اگر **همون کد GA4** را در سایت جدید گذاشتی → دیتا ادامه پیدا می‌کند.
- اگر **کد جدید** ساختی → دیتا از صفر شروع می‌شود.

### ۴. آدرس‌های دیگری که عوض شده؟
اگر قبلاً آدرس‌های دیگری هم داشتی (مثلاً `/privacy.html` یا `/quote.html`) و الان در Django فرق کرده، بگو تا همان‌ها را هم به صورت ۳۰۱ در `main/urls.py` اضافه کنیم تا رتبه نسوزد.

---

## جمع‌بندی

| مورد | وضعیت |
|------|--------|
| Search Console (ثبت مجدد) | ❌ لازم نیست اگر دامنه/پروتکل همان باشد |
| Sitemap جدید | ✅ حتماً `sitemap.xml` را در Sitemaps اضافه کن |
| ریدایرکت ۳۰۱ | ✅ برای `*.html` به مسیر بدون `.html` اضافه شده |
| URL Inspection | ✅ برای چند صفحه مهم Request Indexing بزن |
| Analytics | بستگی به همان کد یا کد جدید دارد |

بعد از دپلو، یک بار `https://YOUR-DOMAIN.com/sitemap.xml` را در مرورگر باز کن و مطمئن شو که لیست URLها درست نمایش داده می‌شود.
